name: deploy-aws

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: Type deploy to run Terraform apply and publish the frontend. Any other value runs plan only.
        required: true
        default: plan

env:
  AWS_REGION: us-east-1
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  TF_INPUT: false

jobs:
  plan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v5
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          role-session-name: aegisdesk-github-plan

      - name: Install Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.14.9"
          terraform_wrapper: false

      - name: Build Lambda package
        run: scripts/build-lambda-package.sh

      - name: Terraform init
        run: terraform -chdir=infra/terraform init

      - name: Terraform validate
        run: terraform -chdir=infra/terraform validate

      - name: Terraform plan
        run: terraform -chdir=infra/terraform plan -out=tfplan

  apply:
    if: inputs.confirm == 'deploy'
    needs: plan
    runs-on: ubuntu-latest
    environment: aws-portfolio
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/web/package-lock.json

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v5
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
          role-session-name: aegisdesk-github-apply

      - name: Install Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: "1.14.9"
          terraform_wrapper: false

      - name: Build Lambda package
        run: scripts/build-lambda-package.sh

      - name: Terraform init
        run: terraform -chdir=infra/terraform init

      - name: Terraform validate
        run: terraform -chdir=infra/terraform validate

      - name: Terraform plan
        run: terraform -chdir=infra/terraform plan -out=tfplan

      - name: Terraform apply
        run: terraform -chdir=infra/terraform apply -auto-approve tfplan

      - name: Install web dependencies
        working-directory: apps/web
        run: npm ci

      - name: Build static frontend
        run: |
          API_URL=$(terraform -chdir=infra/terraform output -raw api_gateway_url)
          NEXT_PUBLIC_API_BASE_URL="$API_URL" npm run build:web

      - name: Publish frontend
        run: |
          BUCKET=$(terraform -chdir=infra/terraform output -raw frontend_bucket_name)
          DIST_ID=$(terraform -chdir=infra/terraform output -raw frontend_distribution_id)
          aws s3 sync apps/web/out "s3://$BUCKET" --delete
          aws s3 cp apps/web/out/marketing.html "s3://$BUCKET/marketing" --content-type text/html
          aws cloudfront create-invalidation --distribution-id "$DIST_ID" --paths "/*"

      - name: Smoke test API
        run: |
          API_URL=$(terraform -chdir=infra/terraform output -raw api_gateway_url)
          curl -fsS "$API_URL/health"
